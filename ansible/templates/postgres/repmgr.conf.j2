node_id = {{ ansible_hostname }}
node_name = 'node{{ ansible_hostname }}'
conninfo = 'host={{ ansible_enp0s8['ipv4']['address'] }} user=repmgr dbname='repmgr'
data_directory = '/var/lib/postgresql/14/main'
use_replication_slots = yes
reconnect_attempts = 5
reconnect_interval = 1
failover = automatic
pg_bindir = '/usr/lib/postgresql/14/bin'
promote_command = 'repmgr standby promote -f /etc/repmgr.conf'
follow_command = 'repmgr standby follow -f /etc/repmgr.conf'
log_level = INFO
log_file = '/var/log/postgresql/repmgr.log'
 
#monitoring_history=yes
#monitor_interval_secs=5
#log_status_interval=5
#promote_check_timeout=5
#promote_check_interval=1
#master_response_timeout=5